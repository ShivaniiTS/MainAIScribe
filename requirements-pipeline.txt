# requirements-pipeline.txt — Processing pipeline server (GPU/ML included)
# Used by: deploy/Dockerfile.pipeline
#
# Install locally (GPU machine with CUDA):
#   pip install -r requirements-pipeline.txt

# ── All provider-facing deps ───────────────────────────────────────────────
-r requirements.txt

# ── ASR / transcription dependencies ─────────────────────────────────────
# Core deep-learning framework (CPU wheels; GPU builds are installed on the host)
torch>=2.0.0
torchaudio>=2.0.0

# faster-whisper: optimised Whisper backend (required by WhisperX)
faster-whisper>=1.0.0

# CTranslate2: inference engine used by faster-whisper
ctranslate2>=4.0.0

# WhisperX: forced-alignment + diarisation wrapper around faster-whisper
# Install from PyPI (>=3.1) or fallback to GitHub source below if unavailable:
# pip install git+https://github.com/m-bain/whisperX.git
whisperx>=3.1.0

# Pyannote diarisation (used by WhisperX for speaker separation)
pyannote.audio>=3.1.0

# Silero VAD (voice activity detection)
pysilero-vad>=0.0.1

# ── ML / GPU dependencies ─────────────────────────────────────────────────
# LoRA fine-tuning
peft>=0.18.1

# HuggingFace datasets
datasets>=4.7.0

# Audio I/O
soundfile>=0.13.1

# Audio augmentation (training data prep)
audiomentations>=0.43.1

# HuggingFace Accelerate (fp16 training)
accelerate>=1.13.0
